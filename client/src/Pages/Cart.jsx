import React from 'react'

function buttonClick() {
    
}

const Cart = () => {
    return (
        <div>
            <button onClick={buttonClick}>Show Items</button>
        </div>
    )
}

export default Cart